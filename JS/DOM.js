console.log('Dom'); //defines a standard for accessing documents
// all the below are in global sclae
let a = window.document; // window object is already global in JS

// alert('HEllo');

// a = prompt('This will DEstroy'); // Thsi is guve a prompt
// a = confirm('Are you sure'); // confirm will return true or false 
a = window.innerHeight;
a = window.innerWidth;
a = innerHeight; // it will automiclly direct it is window

a = scrollX; // it show much you have scrolled in X & Y direction
a = scrollY;

a = location;
a = location.reload;
a = location.toString();
a = window.history;
a = history.length;

console.log(a);


// alert , prompt, confirm are not used so much now


